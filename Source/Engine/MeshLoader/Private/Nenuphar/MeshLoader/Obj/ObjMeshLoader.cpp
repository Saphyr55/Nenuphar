#include "Nenuphar/MeshLoader/Obj/ObjMeshLoader.hpp"

namespace Nenuphar
{
    SharedRef<Mesh> ObjMeshLoader::Load(const File& file)
    {
        if (!)
        {
            return nullptr;
        }

        Path::FilePathData(path);

        return nullptr;    
    }
}